apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocp4_workload_mta_tackle2.fullname" . }}
  labels:
    {{- include "ocp4_workload_mta_tackle2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocp4_workload_mta_tackle2.selectorLabels" . | nindent 4 }}
